<% content_for :title, "Gestion des matières" %>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Gestion des matières</h1>
    <%= link_to new_subject_path, class: "btn btn-success d-flex align-items-center gap-2" do %>
      <i class="bi bi-plus-circle-fill"></i>
      <span>Nouvelle matière</span>
    <% end %>
  </div>

  <% if notice %>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
      <%= notice %>
      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
  <% end %>

  <div class="table-responsive">
    <table class="table table-hover">
      <thead class="table-light">
        <tr>
          <th>Nom</th>
          <th>Description</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        <% @subjects.each do |subject| %>
          <tr>
            <td><%= subject.name %></td>
            <td><%= subject.description %></td>
            <td>
              <div class="d-flex gap-2 justify-content-end">
                <%= link_to subject, class: "btn btn-sm btn-primary", title: "Voir" do %>
                  <i class="bi bi-eye-fill"></i>
                <% end %>

                <%= link_to edit_subject_path(subject), class: "btn btn-sm btn-warning", title: "Modifier" do %>
                  <i class="bi bi-pencil-fill text-white"></i>
                <% end %>

                <%= button_to subject_path(subject), 
                    method: :delete, 
                    class: "btn btn-sm btn-danger", 
                    title: "Supprimer",
                    form: { 
                      class: "d-inline-block",
                      data: { turbo_confirm: "Êtes-vous sûr de vouloir supprimer cette matière ?" } 
                    } do %>
                  <i class="bi bi-trash-fill"></i>
                <% end %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<% content_for :title, "Modification de la personne" %>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Modification de la personne</h1>
  </div>

  <%= render "form", person: @person %>

  <div class="mt-3">
    <%= link_to "Voir cette personne", @person, class: "btn btn-info" %> |
    <%= link_to "Retour à la liste", people_path, class: "btn btn-secondary" %>
  </div>
</div>
